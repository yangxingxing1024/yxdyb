server:
  port: 8080

spring:
  datasource:
    name: test
    url: jdbc:mysql://127.0.0.1:3306/jtsys
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  mvc:
    static-path-pattern: /static/**
  resources:
    static-locations: classpath:/static/

mybatis:
  mapperLocations: classpath*:mapper/*.xml
  type-aliases-package: com.example.dyb.register.entity

